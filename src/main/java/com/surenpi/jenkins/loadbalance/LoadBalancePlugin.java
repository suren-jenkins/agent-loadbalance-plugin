package com.surenpi.jenkins.loadbalance;

import hudson.Plugin;

/**
 * @author suren
 */
public class LoadBalancePlugin extends Plugin
{
    @Override
    public void start() throws Exception
    {
//        Jenkins jenkins = Jenkins.getInstance();
//        Queue queue = jenkins.getQueue();
//
//        LoadBalancer loadBalancer = queue.getLoadBalancer();
//        LoadBalancer computeResourceBalancer = new ComputeResourceBalancer(loadBalancer);
//
//        queue.setLoadBalancer(computeResourceBalancer);
    }
}